Folder ini adalah tempat untuk project inti Laravel. 

Nantinya di VPS, Anda bisa melakukan clone atau pull repositori di dalam folder ini:
`git clone <url-repo-anda> .`

Pastikan file `artisan` dan folder `public` dll berada langsung di dalam folder `src` ini agar volume docker di set-up dengan benar.
